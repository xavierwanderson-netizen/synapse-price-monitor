[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "node index.js"
restartPolicy = "always"
healthcheckPath = ""
healthcheckInterval = 10
healthcheckTimeout = 5

[networking]
# Sem porta pública - worker contínuo apenas
# Public networking é desativado para workers
